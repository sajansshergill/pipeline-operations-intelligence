# Streamlit Community Cloud (often Python 3.14 + uv): pin wheels that support cp314.
# Old stack (Streamlit 1.34 + Pillow 10.4) had no Pillow 3.14 wheel → zlib source-build failure.

pillow==12.2.0
streamlit==1.56.0
duckdb==1.5.1
pandas==3.0.2
plotly==5.24.1
python-dotenv==1.0.1
