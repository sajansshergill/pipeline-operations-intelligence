# Vertex Ops Hub — Streamlit dashboard only (Streamlit Community Cloud default file).
# No Docker, Kafka, Spark, or Airflow required to view the app.
# Bundled data/vertex_ops.duckdb supplies charts out of the box.
#
# Full pipeline: pip install -r requirements-pipeline.txt

streamlit==1.34.0
duckdb==0.10.3
pandas==2.2.2
plotly==5.22.0
python-dotenv==1.0.1
